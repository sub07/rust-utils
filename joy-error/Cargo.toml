[package]
name = "joy-error"
version = "0.3.0"
edition = "2021"

[dependencies]
log = { version = "0", optional = true }
tracing = { version = "0", optional = true }
easy-ext = "0"

[features]
default = []
tracing-crate = ["dep:tracing"]
log-crate = ["dep:log"]
